<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_quick_record"
            android:icon="@drawable/ic_quick_record"
            android:title="@string/quick_record" />
        <item
            android:id="@+id/nav_manual_record"
            android:icon="@drawable/ic_manual_record"
            android:title="@string/manual_record" />
    </group>

    <item android:title="@string/nav_title_summary">
        <menu>
            <group android:checkableBehavior="single">
                <item
                    android:id="@+id/nav_my_records"
                    android:icon="@drawable/ic_record_list"
                    android:title="@string/my_records" />
                <item
                    android:id="@+id/nav_about"
                    android:icon="@drawable/ic_menu_send"
                    android:title="About" />
            </group>
        </menu>
    </item>

</menu>
